## Полезные команды
```
# Шпаргалка по CRON JOB 

Cron - мощный инструмент для автоматического запуска команд и скриптов по расписанию. Вот основные моменты, которые нужно знать:

🔹 Формат cron-записи:


* * * * * username command
│ │ │ │ │
│ │ │ │ └─ День недели (0-6, где 0 = Воскресенье)
│ │ │ └─── Месяц (1-12)
│ │ └───── День месяца (1-31)
│ └─────── Час (0-23)
└───────── Минута (0-59)


✨ Примеры:

 0 0 * * * - запуск каждый день в полночь 🌙
 */5 * * * * - запуск каждые 5 минут
 0 */6 * * * - запуск каждые 6 часов
 5 12 * * 0 - запуск по воскресеньям в 12:05

🔹 Часто используемые шорткоды:

- @reboot - при старте системы
- @hourly - каждый час
- @daily - каждый день
- @weekly - каждую неделю
- @monthly - каждый месяц
- @yearly - раз в год

⚙️ Управление cron:

- crontab -e - редактировать задачи
- crontab -l - список задач
- crontab -r - удалить задачи

💡 Пример:
Автоматическое резервное копирование каждый день в 3:30 ночи:


30 3 * * * /home/user/backup.sh
